# Align Assignments

할당 연산자를 열에 맞춰 정렬합니다.

## Before

```text
a = 1
longer = 2
mid = 3
```

## After

```text
a      = 1
longer = 2
mid    = 3
```

## Command

```
%s=<enter>&
```

1. `%` 전체 파일 선택
1. `s` 정규식으로 일치 항목 선택
1. `=` 등호 문자 입력
1. `<enter>` 정규식 확정하고 모든 등호 선택
1. `&` 선택 영역을 열에 맞춰 정렬
