# Replace an Identifier

커서가 놓인 변수 이름을 다른 이름으로 변경합니다.

## Before

```rs
count
```

## After

```rs
total
```

## Command

```
miwctotal<esc>
```

1. `miw` 단어 내부를 선택
1. `c` 선택 영역을 변경
1. `total` 새 변수 이름 입력
1. `<esc>` 일반 모드로 돌아가기
