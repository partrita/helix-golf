# Replace an Identifier

커서가 놓인 변수 이름을 다른 이름으로 변경합니다.

## Before

```rs
count
```

## After

```rs
total
```

## Command

```
ciwtotal<esc>
```

1. `ciw` 현재 단어를 선택하여 삽입 모드로 변경
1. `total` 새 변수 이름 입력
1. `<esc>` 일반 모드로 돌아가기
