# Contributing Helix Golf Examples

이 저장소에 새로운 Helix Golf 문제(예제)를 추가할 때는 아래 규칙을 따라주세요.

## 문제 구성

- 모든 문제는 `src/<problem_name>.md`에 추가합니다.
- 파일 이름은 소문자 `snake_case`를 사용하고, 예제의 핵심 작업을 짧게 표현합니다.
- 제목은 사용자가 수행할 편집 작업을 명확하게 설명합니다.
- 설명은 한두 문장으로 작성하고, Helix의 특정 기능이나 편집 목표가 드러나게 합니다.
- 반드시 다음 네 개의 섹션을 순서대로 포함합니다.
  1. `## Before`
  2. `## After`
  3. `## Command`
  4. `## Steps`는 필요할 때만 추가합니다. 기존 예제처럼 `Command` 아래 번호 목록으로 단계를 설명해도 됩니다.

## Before / After

- `Before`는 실제로 Helix에서 편집할 원본이어야 합니다.
- `After`는 문제에서 요구하는 최종 상태여야 합니다.
- 코드 블록의 언어 태그를 실제 파일 형식에 맞게 지정합니다. 예: `rs`, `py`, `js`, `toml`.
- 가능하면 하나의 명확한 편집 목표에 집중합니다. 지나치게 많은 조작을 한 문제에 넣지 않습니다.
- Before와 After의 차이는 Helix의 편집 기능을 연습할 수 있도록 충분히 의미 있어야 합니다.

## Command

- 실제 Helix에서 재현 가능한 키 입력만 기록합니다.
- 여러 키 입력을 한 줄에 이어 쓸 수 있으며, `<space>`, `<esc>`, `<ret>`, `<alt-s>`처럼 특수 키는 명확한 표기를 사용합니다.
- 선택 영역을 만드는 명령과 실제 변경 명령을 분리하면 읽기 쉽습니다.
- 명령은 반드시 `Before`에서 `After`로 정확히 변환되어야 합니다.
- 복잡한 명령을 추가했다면 번호 목록으로 각 키 입력의 역할을 설명합니다.

## 검증

문제를 추가하거나 수정한 뒤 프로젝트 루트에서 다음 명령으로 구조를 검사합니다:

```sh
cargo validate
```

가능하면 데모도 생성하여 실제 Helix 동작을 확인합니다:

```sh
cargo generate-demos <problem_name>
```

생성된 데모 파일은 소스 예제의 일부가 아니라 생성 산출물일 수 있으므로, 프로젝트의 기존 생성물 관리 방식에 맞춰 처리합니다.

## 체크리스트

새 문제를 제출하기 전에 확인하세요.

- [ ] `src/<problem_name>.md` 파일명이 `snake_case`인가?
- [ ] 제목과 설명만 읽어도 편집 목표가 이해되는가?
- [ ] `Before`와 `After`가 서로 정확히 대응하는가?
- [ ] `Command`가 실제 Helix 키 입력인가?
- [ ] 각 명령의 목적을 설명했는가?
- [ ] `cargo validate`가 통과하는가?
- [ ] `cargo generate-demos <problem_name>`로 데모를 생성하고 동작을 확인했는가?
